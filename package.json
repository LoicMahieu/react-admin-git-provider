{
  "name": "react-admin-gitlab-provider",
  "version": "0.0.4",
  "main": "./lib/index.js",
  "author": "Loïc Mahieu <mahieuloic@gmail.com>",
  "license": "MIT",
  "scripts": {
    "test": "npm run lint && npm run build && jest",
    "lint": "tslint -p .",
    "build": "rm -rf lib/* && tsc",
    "watch": "yarn build && tsc -w"
  },
  "peerDependencies": {
    "react": "^16.7.0"
  },
  "dependencies": {
    "gitlab": "^5.0.0-rc.7",
    "uuid": "^3.3.2"
  },
  "devDependencies": {
    "@types/uuid": "^3.4.4",
    "patch-package": "^5.1.1",
    "prettier": "^1.15.3",
    "tslint": "^5.12.0",
    "typescript": "^3.2.2"
  }
}
