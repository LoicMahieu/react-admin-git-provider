{
  "name": "@react-admin-git-provider/gitlab",
  "version": "0.0.1",
  "files": [
    "lib/*"
  ],
  "main": "./lib/index.js",
  "typings": "./lib/index.d.ts",
  "author": "Loïc Mahieu <mahieuloic@gmail.com>",
  "license": "MIT",
  "scripts": {
    "test": "npm run build",
    "build": "rm -rf lib/* && tsc",
    "watch": "yarn build && tsc -w",
    "prepare": "yarn build"
  },
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "@react-admin-git-provider/common": "^0.0.1",
    "gitlab": "LoicMahieu/node-gitlab#feature/request-ky-build",
    "p-limit": "^2.2.0"
  },
  "devDependencies": {
    "@types/jest": "^24.0.6",
    "@types/p-limit": "^2.0.0",
    "jest": "^24.1.0",
    "ts-jest": "^23.10.5",
    "typescript": "^3.3.3"
  }
}
